# OpenAI API Configuration (Server-side only - NOT exposed to frontend)
# This should be set in Supabase Edge Functions environment variables
# OPENAI_API_KEY="your-openai-api-key"

# SECURITY NOTE: OpenAI API key is now handled server-side for security
# Set OPENAI_API_KEY in your Supabase Edge Functions environment variables
# You can get your OpenAI API key from: https://platform.openai.com/api-keys
# Make sure you have credits in your OpenAI account for the API to work 

# Supabase Configuration
VITE_SUPABASE_URL="your-supabase-url"
VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"

# Site URL (for webhooks and redirects)
SITE_URL="http://localhost:8080" 